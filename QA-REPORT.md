# 기능 QA 리포트 — Antigravity Rehab Dashboard

실행: /qa (Claude Preview MCP, 깨끗한 컨텍스트) · 2026-06-01
목적: design-review("보기 좋은가") 이후 "실제로 다 작동하는가" + 최근 변경(SRS·개념렌즈·반응형) 회귀 점검

## 결과 요약
**실 버그 0건.** 7개 영역 전 기능 검증 통과, 콘솔 에러 0. 기능 건전성: **A (ship-ready)**.

## 검증한 영역

| 영역 | 테스트 | 결과 |
|------|--------|------|
| **대시보드** | 테마토글+영속성, 실행방법 모달, 용어사전(Ctrl+K, 137카드), 진척도 위젯 | ✅ |
| **검색(lunr)** | focus→인덱스 빌드(205 docs)→sessionStorage 캐시→"JWT" 6결과(챕터 컨텍스트)→클릭 네비 | ✅ |
| **퀴즈 엔진** | MCQ 정답/오답/정답후잠금, 코드 validator 채점, 10카드 렌더 | ✅ |
| **챕터 렌더** | Mermaid 3/3 SVG(ch2), 용어 자동태깅 10개, 일러스트, 진척도 체크박스→localStorage | ✅ |
| **크로스페이지 상태** | 챕터 진척도 저장 → 대시보드 6%·링·카드상태 반영 | ✅ |
| **demo-shop** | 상품 로드, 장바구니 담기, 개념렌즈 토글, 로그인폼/REST로그/API계측 | ✅ |
| **review.html(SRS)** | 라이트 기본, 카드 렌더, 페이저, 초기화 버튼, 신규 캡 15 | ✅ |

## 버그처럼 보였으나 아니었던 것
- **검색 "인덱스 준비 중..." 지속** → 테스트 아티팩트. `search.js`는 인덱스 빌드를 `focus` 이벤트에서만 트리거(63행). QA에서 `input`만 디스패치하고 `focus`를 안 줘서였음. 실제 사용자는 클릭(=focus)으로 빌드 시작 → focus 디스패치 후 205-doc 인덱스 정상 빌드·검색 확인.

## 경미한 UX 관찰 (버그 아님, 조치 불요)
- 검색 인덱스는 **첫 focus 시 15개 HTML을 fetch**해 빌드(~1-2s). 그동안 "인덱스 준비 중" + 토스트로 안내됨(의도된 동작, sessionStorage 캐시로 1회만). 첫 사용 시 즉시 타이핑하면 잠깐 대기.
- (유지보수 메모) 신규 페이지 추가 시 `search.js`의 `INDEX_PAGES` 배열에도 등록해야 검색됨.

## 결론
최근 모든 기능(SRS v1~v1.6, 개념렌즈, 반응형 수정)을 빌드 중 매번 라이브 검증한 효과로 **회귀 0**. 기능적으로 ship-ready. 별도 수정 없음.
